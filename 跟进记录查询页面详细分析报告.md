# 跟进记录查询页面详细分析报告

## 目录
1. [概述](#概述)
2. [文件结构分析](#文件结构分析)
3. [数据库设计](#数据库设计)
4. [核心功能实现](#核心功能实现)
5. [前端交互流程](#前端交互流程)
6. [权限控制机制](#权限控制机制)
7. [代码详细解析](#代码详细解析)
8. [安全与性能分析](#安全与性能分析)
9. [改进建议](#改进建议)

---

## 概述

跟进记录查询页面是CRM系统中用于查询和管理客户跟进记录的核心功能模块。该页面支持多条件搜索、分页显示、排序等功能，并实现了基于角色的权限控制。

**主要功能：**
- 多条件组合查询（客户名称、跟进人员、意向状态等）
- 分页显示查询结果
- 列排序功能
- 权限控制（管理员/普通用户）
- 跟进记录详情查看/编辑/删除

---

## 文件结构分析

### 核心文件清单

#### 1. list_flow.php - 列表展示页面（主入口）
**路径：** `ori/MYOA/webroot/general/customer_relationship/customer_data/list_flow.php`

**职责：**
- 提供搜索表单界面
- 初始化页面并加载数据
- 处理前端交互逻辑

**关键代码结构：**
```php
<?php
include_once("inc/utility_all.php");
include_once("inc/utility_org.php");
include_once("lang.php");
// ... 其他include

// JavaScript函数
function loadList(page) {
    // AJAX加载列表数据
}

function sortList(obj) {
    // 实现列排序功能
}
?>
```

#### 2. query_flow.php - 查询处理页面
**路径：** `ori/MYOA/webroot/general/customer_relationship/customer_data/query_flow.php`

**职责：**
- 接收查询参数
- 构建SQL查询语句
- 执行数据库查询
- 返回HTML表格片段

**关键流程：**
1. 接收GET/POST参数
2. 构建WHERE条件
3. 应用权限过滤
4. 执行分页查询
5. 生成HTML表格

#### 3. flow.php - 详情页面
**路径：** `ori/MYOA/webroot/general/customer_relationship/customer_data/flow.php`

**职责：**
- 显示单个客户的所有跟进记录
- 展示客户基本信息
- 展示联系人信息
- 展示跟进记录列表

#### 4. ActionModel.php - 数据模型类
**路径：** `ori/MYOA/webroot/general/customer_relationship/customer_data/ActionModel.php`

**关键方法：**
- `queryFlow($param)` - 查询单个客户的跟进记录

---

## 数据库设计

### 表结构：inhe_customer_data_flow

```sql
CREATE TABLE `inhe_customer_data_flow` (
  `id` int(11) unsigned NOT NULL AUTO_INCREMENT,
  `form_id` varchar(100) DEFAULT NULL COMMENT '客户表流程单号',
  `customer_id` int(11) DEFAULT NULL COMMENT '客户表id',
  `flow_date` date DEFAULT NULL COMMENT '跟进日期',
  `create_time` datetime DEFAULT NULL COMMENT '创建时间',
  `remark` text COMMENT '备注（商谈事项）',
  `user_id` varchar(50) DEFAULT NULL COMMENT '账号',
  `user_name` varchar(100) DEFAULT NULL COMMENT '姓名（跟进人员）',
  `flow_type` varchar(255) DEFAULT NULL COMMENT '跟进方式',
  `ATTACH_NAME` text COMMENT '附件',
  `ATTACH_ID` text COMMENT '附件id',
  `contract_id` int(11) DEFAULT NULL COMMENT '关联联系人',
  `contract_name` varchar(255) DEFAULT NULL COMMENT '联系人名称',
  `order_num` int(11) DEFAULT NULL COMMENT '订单数量（总数量）',
  `order_will` varchar(1) DEFAULT NULL COMMENT '是否有订单意愿 Y N',
  `need` varchar(255) DEFAULT NULL COMMENT '需求',
  `purma` varchar(255) DEFAULT NULL COMMENT '供应商',
  `dept_id` int(11) DEFAULT NULL COMMENT '部门id',
  `user_bu` varchar(255) DEFAULT NULL COMMENT '公司别名',
  PRIMARY KEY (`id`)
) ENGINE=InnoDB AUTO_INCREMENT=1257 DEFAULT CHARSET=gbk;
```

### 字段说明

| 字段名 | 类型 | 说明 | 对应表格列 |
|--------|------|------|-----------|
| id | int(11) | 主键，自增 | - |
| form_id | varchar(100) | 客户表流程单号，关联inhe_customer_data表 | - |
| customer_id | int(11) | 客户表ID | - |
| flow_date | date | 跟进日期 | 跟进日期 |
| create_time | datetime | 创建时间 | 创建时间 |
| remark | text | 备注/商谈事项 | 商谈事项 |
| user_id | varchar(50) | 跟进人员账号 | - |
| user_name | varchar(100) | 跟进人员姓名 | 跟进人员 |
| contract_name | varchar(255) | 联系人名称 | 联系人 |
| order_num | int(11) | 订单数量 | 总数量 |
| order_will | varchar(1) | 是否有意向采购产品（Y/N） | 是否有意向采购产品 |
| dept_id | int(11) | 部门ID | - |
| user_bu | varchar(255) | 公司别名 | - |

### 关联表

**inhe_customer_data** - 客户信息表
- 关联字段：`form_id`
- 用途：获取客户名称（customer_name）

**user** - 用户表
- 关联字段：`user_id`
- 用途：获取用户工号（user_byid）

---

## 核心功能实现

### 1. 查询条件构建（query_flow.php 第20-41行）

```php
$whereStr = " where 1=1 ";

// 客户名称（模糊查询）
if (array_key_exists('customer_name', $param) && $param['customer_name'] != "") {
    $whereStr .= " and d.customer_name like '%" . trim($param['customer_name']) . "%' ";
}

// 跟进人员（模糊查询）
if (array_key_exists('user_name', $param) && $param['user_name'] != "") {
    $whereStr .= " and k.user_name like '%" . trim($param['user_name']) . "%' ";
}

// 用户工号（精确查询）
if (array_key_exists('user_byid', $param) && $param['user_byid'] != "") {
    $whereStr .= " and u.user_byid = '" . trim($param['user_byid']) . "' ";
}

// 是否有意向采购产品
if (array_key_exists('order_will', $param) && $param['order_will'] != "") {
    $whereStr .= " and k.order_will = '" . $param['order_will'] . "' ";
}

// 时间范围查询
if (array_key_exists('starting_time', $param) && $param['starting_time'] != "") {
    $whereStr .= " and k.flow_date >= '" . $param['starting_time'] . "' ";
}
if (array_key_exists('ending_time', $param) && $param['ending_time'] != "") {
    $end_day = date('Y-m-d', strtotime("+1 day", strtotime($param['ending_time'])));
    $whereStr .= " and k.flow_date < '" . $end_day . "' ";
}
```

**支持的查询参数：**
- `customer_name` - 客户名称（LIKE查询）
- `user_name` - 跟进人员（LIKE查询）
- `user_byid` - 用户工号（精确匹配）
- `order_will` - 是否有意向采购产品（Y/N）
- `starting_time` - 跟进日期开始时间
- `ending_time` - 跟进日期结束时间

### 2. SQL查询语句（query_flow.php 第74-78行）

```php
$totalQuery = "select d.customer_name,k.* from inhe_customer_data_flow k " .
    " left join inhe_customer_data d on k.form_id = d.form_id ".
    " left join user u on u.user_id = k.user_id " .
    $whereStr . $groupBy . $orderBy;
$query = $totalQuery . $limit;
```

**查询逻辑：**
1. 主表：`inhe_customer_data_flow` (别名 k)
2. 左连接：`inhe_customer_data` (别名 d) - 获取客户名称
3. 左连接：`user` (别名 u) - 获取用户信息
4. 应用WHERE条件
5. 应用排序
6. 应用分页限制

### 3. 分页实现（query_flow.php 第62-65行）

```php
$groupBy = $limit = "";
$orderBy = " order by k.id desc ";
$groupBy = " ";
$limit = " limit " . ($param['page'] - 1) * $param['page_size'] . "," . $param['page_size'];
```

**分页参数：**
- `page` - 当前页码（从1开始）
- `page_size` - 每页记录数（默认10）

**分页计算：**
- 偏移量 = (page - 1) × page_size
- LIMIT语法：`LIMIT offset, count`

### 4. 排序功能（query_flow.php 第67-72行）

```php
$laySort = array();
if (array_key_exists('sort_field', $param) && $param['sort_field'] != "" 
    && array_key_exists('sort_way', $param) && $param['sort_way'] != "") {
    $orderBy = " order by " . $param['sort_field'] . " " . $param['sort_way'];
    $laySort[$param['sort_field']] = $param['sort_way'];
}
```

**排序参数：**
- `sort_field` - 排序字段（如：d.customer_name）
- `sort_way` - 排序方式（asc/desc）

**默认排序：** 按 `k.id desc` 降序

---

## 前端交互流程

### 1. 页面初始化（list_flow.php 第23-29行）

```javascript
$(function() {
    layui.use(['form', 'layer', 'laydate'], function() {
        var layer = layui.layer,
            form = layui.form;
        loadList('1');  // 加载第一页数据
        form.render();
    });
    
    // 点击备注内容弹窗显示完整内容
    $(document).on("click","#remarks",function(e) {
        if (this.offsetWidth < this.scrollWidth || this.innerHTML.length>=1) {
            var text = $(this).attr('title');
            layer.alert(text);
        }
    });
});
```

### 2. 数据加载函数（list_flow.php 第39-46行）

```javascript
function loadList(page) {
    var type = $("#type").val();
    $("#tableList").empty();
    $("#tableList").html(
        '<div><img src="/static/images/loading.gif" align="absmiddle" />加载中...</div>'
    );
    $("#tableList").load("query_flow.php?" + $("#tab").serialize() + "&page=" + page + "&type=" + type, function() {});
}
```

**流程：**
1. 获取表单类型
2. 清空列表容器
3. 显示加载动画
4. 通过AJAX加载 `query_flow.php` 返回的HTML
5. 将HTML插入到 `#tableList` 容器

### 3. 排序功能（list_flow.php 第48-57行）

```javascript
function sortList(obj) {
    myObj = $(obj).parent();
    var sortWay = $(obj).attr("sort-way");
    var sortField = $(obj).parent().attr("sort-field");
    $(obj).parent().attr("lay-sort", sortField);
    $("#sort_way").val(sortWay);
    $("#sort_field").val(sortField);
    loadList('1');  // 重新加载第一页
}
```

**流程：**
1. 获取排序字段和排序方式
2. 更新隐藏表单字段
3. 重新加载第一页数据

### 4. 搜索表单（list_flow.php 第84-108行）

```html
<form id="tab" class="layui-form">
    <table style="margin: 1%">
        <tr>
            <td align="right">客户名称：</td>
            <td>
                <input type="text" name="customer_name" id="customer_name" 
                       value="<?=$customer_name?>" class="layui-input">
            </td>
            <td align="right">跟进人员：</td>
            <td>
                <input type="text" name="user_name" id="user_name" 
                       class="layui-input" value="<?=$user_name?>">
            </td>
            <td align="right">是否有意向采购产品：</td>
            <td>
                <select id="order_will" name="order_will">
                    <option value=''>请选择</option>
                    <option value='Y'>是</option>
                    <option value='N'>否</option>
                </select>
            </td>
            <td>
                <button class="layui-btn" onclick="loadList('1');return false;">查询</button>
            </td>
        </tr>
    </table>
</form>
```

### 5. 数据表格展示（query_flow.php 第94-153行）

**表格列结构：**

| 序号 | 列名 | 数据来源 | 说明 |
|------|------|---------|------|
| 1 | 序号 | 计算字段 | `$line + ($param['page'] - 1) * $param['page_size']` |
| 2 | 客户名称 | `d.customer_name` | 来自关联表 |
| 3 | 跟进人员 | `k.user_name` | 跟进记录表 |
| 4 | 跟进日期 | `k.flow_date` | 跟进记录表 |
| 5 | 联系人 | `k.contract_name` | 跟进记录表 |
| 6 | 是否有意向采购产品 | `k.order_will` | Y显示"是"，N显示"否" |
| 7 | 总数量 | `k.order_num` | 跟进记录表 |
| 8 | 创建时间 | `k.create_time` | 跟进记录表 |
| 9 | 商谈事项 | `k.remark` | 支持点击查看完整内容 |
| 10 | 操作 | - | 详情/修改/删除按钮 |

**表格渲染代码：**

```php
<?php $line = 0;
foreach ($result as $k => $val) : $line++; ?>
    <tr class="TableLine<?= $line % 2 == 0 ? 1 : 2 ?>">
        <td align="center" width="3%">
            <?= $line + ($param['page'] - 1) * $param['page_size'] ?>
        </td>
        <td align="center"><?= $val["customer_name"] ?></td>
        <td align="center"><?= $val["user_name"] ?></td>
        <td align="center"><?= $val["flow_date"] ?></td>
        <td align="center"><?= $val["contract_name"] ?></td>
        <td align="center">
            <?= $val["order_will"]=="Y" ? "是" : "否" ?>
        </td>
        <td align="center"><?= $val["order_num"] ?></td>
        <td align="center"><?= $val["create_time"] ?></td>
        <td nowrap align="left">
            <p id="remarks" class="content" title="<?= $val["remark"] ?>">
                <?= $val["remark"] ?>
            </p>
        </td>
        <td align="center" nowrap>
            <a href="javascript:void(0);" class="newColor" 
               onclick="pageOperate('flow_detail.php?id=<?= $val[id] ?>','detail');">
                详情
            </a>&nbsp;
            <?php if ($userId=="admin"||$userId=="INHE-1008") : ?>
                <a href="javascript:void(0);" class="newColor" 
                   onclick="pageOperate('flow_modify.php?id=<?= $val[id] ?>','modify');">
                    修改
                </a>&nbsp;
                <a href="javascript:void(0);" class="newColor" 
                   onclick="pageOperate('flow_delete.php&id=<?= $val[id] ?>','delete');">
                    删除
                </a>
            <?php endif; ?>
        </td>
    </tr>
<?php endforeach; ?>
```

---

## 权限控制机制

### 权限判断（query_flow.php 第13-18行）

```php
$userId = $_SESSION['LOGIN_USER_ID'];
$deptId = $_SESSION['LOGIN_DEPT_ID'];
$systemId = "customer_data";
$data = $userModel->getAdminDataQuery($userId, $deptId, $systemId);
$isAdmin = $data['isAdmin'];
$systemAdmin = $data['systemAdmin'];
```

### 权限过滤逻辑（query_flow.php 第43-60行）

```php
// 权限范围控制
if ($isAdmin) {
    // 管理员权限：可查看自己的记录 + 同公司别名 + 同部门 + 指定用户
    $whereStr .= " and (k.user_id = '" . $userId . "' ";
    if (!empty($data['userBu'])) {
        $whereStr .= " or k.user_bu in (" . $data['userBu'] . ") ";
    }
    if (!empty($data['deptIds'])) {
        $whereStr .= " or k.dept_id in(" . $data['deptIds'] . ") ";
    }
    if (!empty($data['userIds'])) {
        $whereStr .= " or k.user_id in(" . $data['userIds'] . ") ";
    }
} else {
    // 普通用户权限：只能查看自己的记录
    $whereStr .= " and (k.user_id = '" . $userId . "' ";
}
$whereStr .= ") ";
```

**权限规则：**

| 用户类型 | 可查看范围 |
|---------|-----------|
| 管理员 | 自己的记录 + 同公司别名记录 + 同部门记录 + 指定用户记录 |
| 普通用户 | 仅自己的记录 |

### 操作按钮权限（query_flow.php 第142-147行）

```php
<?php if ($userId=="admin"||$userId=="INHE-1008") : ?>
    <a href="javascript:void(0);" onclick="pageOperate('flow_modify.php?id=<?= $val[id] ?>','modify');">
        修改
    </a>&nbsp;
    <a href="javascript:void(0);" onclick="pageOperate('flow_delete.php&id=<?= $val[id] ?>','delete');">
        删除
    </a>
<?php endif; ?>
```

**操作权限：**
- **详情**：所有用户可见
- **修改/删除**：仅 `admin` 或 `INHE-1008` 用户可见

---

## 代码详细解析

### 1. ActionModel::queryFlow() 方法

**位置：** `ActionModel.php` 第81-99行

```php
public function queryFlow($param)
{
    $result = array();
    $strWhere = " where 1=1 ";
    
    // 参数验证
    if (!array_key_exists("id", $param) || $param['id'] == "") {
        return $result;
    }
    
    // 处理关联ID
    if ($param['related_id'] != "") {
        $param['id'] = $param['related_id'];
    }
    
    // 构建查询条件
    $strWhere .= " and k.form_id = '" . $param['id'] . "' ";
    
    // 执行查询
    $sql = " select k.* from inhe_customer_data_flow k " . $strWhere;
    $res = exequery(TD::conn(), $sql);
    
    // 获取结果
    while ($item = mysql_fetch_assoc($res)) {
        $result[] = $item;
    }
    mysql_free_result($res);
    
    return $result;
}
```

**用途：** 查询指定客户（form_id）的所有跟进记录

### 2. 备注内容显示优化

**CSS样式（list_flow.php 第60-67行）：**

```css
.table #remarks {
    white-space: pre-line;        /* 保留换行 */
    overflow: hidden;             /* 隐藏溢出 */
    text-overflow: ellipsis;       /* 超出显示省略号 */
    display: -webkit-box;          /* 使用弹性盒子 */
    -webkit-line-clamp: 3;         /* 最多显示3行 */
    -webkit-box-orient: vertical;   /* 垂直方向 */
}

table {
    table-layout: fixed;           /* 固定表格布局 */
    word-wrap: break-word;         /* 长单词换行 */
}
```

**交互功能：** 点击备注内容可弹窗显示完整内容

### 3. 分页组件

**分页条生成（query_flow.php 第90-92行）：**

```php
<div style="text-align: right;width: 98%;margin:auto;">
    <?= $model->page_bar($param['page'], $count, $param['page_size'], 'loadList(\'%s\')', '1') ?>
</div>
```

**参数说明：**
- `$param['page']` - 当前页码
- `$count` - 总记录数
- `$param['page_size']` - 每页记录数
- `'loadList(\'%s\')'` - 分页点击回调函数
- `'1'` - 其他参数

---

## 安全与性能分析

### 安全风险

#### 1. SQL注入风险 ⚠️

**问题代码：**

```php
// query_flow.php 第23行
$whereStr .= " and d.customer_name like '%" . trim($param['customer_name']) . "%' ";

// query_flow.php 第26行
$whereStr .= " and k.user_name like '%" . trim($param['user_name']) . "%' ";

// query_flow.php 第29行
$whereStr .= " and u.user_byid = '" . trim($param['user_byid']) . "' ";

// query_flow.php 第32行
$whereStr .= " and k.order_will = '" . $param['order_will'] . "' ";
```

**风险说明：**
- 直接拼接用户输入到SQL语句中
- 未使用参数化查询或预处理语句
- 恶意用户可能通过输入特殊字符执行SQL注入攻击

**修复建议：**

```php
// 使用预处理语句
$stmt = $pdo->prepare("SELECT d.customer_name, k.* 
                       FROM inhe_customer_data_flow k 
                       LEFT JOIN inhe_customer_data d ON k.form_id = d.form_id 
                       WHERE d.customer_name LIKE ?");
$stmt->execute(["%" . $param['customer_name'] . "%"]);
```

#### 2. XSS跨站脚本攻击风险 ⚠️

**问题代码：**

```php
// query_flow.php 第127行
<td align="center"><?= $val["customer_name"] ?></td>

// query_flow.php 第134行
<p id="remarks" class="content" title="<?= $val["remark"] ?>">
    <?= $val["remark"] ?>
</p>
```

**风险说明：**
- 直接输出数据库内容到HTML
- 如果数据库中存在恶意脚本，可能被执行

**修复建议：**

```php
// 使用htmlspecialchars转义
<td align="center"><?= htmlspecialchars($val["customer_name"], ENT_QUOTES, 'UTF-8') ?></td>

<p id="remarks" class="content" title="<?= htmlspecialchars($val["remark"], ENT_QUOTES, 'UTF-8') ?>">
    <?= htmlspecialchars($val["remark"], ENT_QUOTES, 'UTF-8') ?>
</p>
```

#### 3. 权限控制不完善 ⚠️

**问题：**
- 操作按钮权限仅通过前端判断（第142行）
- 后端未验证删除/修改权限
- 可能存在越权操作风险

**修复建议：**
- 在后端API中验证操作权限
- 使用统一的权限验证中间件

### 性能优化

#### 1. 数据库索引建议

**建议添加索引：**

```sql
-- 客户名称查询索引
CREATE INDEX idx_customer_name ON inhe_customer_data(customer_name);

-- 跟进日期查询索引
CREATE INDEX idx_flow_date ON inhe_customer_data_flow(flow_date);

-- 用户ID查询索引
CREATE INDEX idx_user_id ON inhe_customer_data_flow(user_id);

-- 表单ID关联索引（已存在但需确认）
CREATE INDEX idx_form_id ON inhe_customer_data_flow(form_id);

-- 组合索引（用于常用查询组合）
CREATE INDEX idx_user_date ON inhe_customer_data_flow(user_id, flow_date);
```

#### 2. 查询优化

**当前问题：**
- 使用 `mysql_num_rows()` 统计总数，需要执行完整查询
- 对于大数据量，性能较差

**优化建议：**

```php
// 使用COUNT(*)统计总数，避免查询所有数据
$countQuery = "SELECT COUNT(*) as total FROM inhe_customer_data_flow k 
               LEFT JOIN inhe_customer_data d ON k.form_id = d.form_id 
               " . $whereStr;
$countRes = exequery(TD::conn(), $countQuery);
$countRow = mysql_fetch_assoc($countRes);
$count = $countRow['total'];
```

#### 3. 缓存优化

**建议：**
- 对常用查询结果进行缓存
- 使用Redis缓存客户名称映射关系
- 缓存权限数据，减少数据库查询

---

## 改进建议

### 1. 代码重构建议

#### 分离关注点
- 将SQL查询逻辑封装到Model层
- 将权限验证逻辑提取为独立方法
- 将HTML生成逻辑分离到View层

#### 使用MVC架构
```
Controller: FlowController
  - list() - 列表页面
  - query() - 查询接口
  - detail() - 详情页面

Model: FlowModel
  - queryList($params) - 查询列表
  - queryById($id) - 查询单条
  - validatePermission($userId, $flowId) - 权限验证

View: flow/list.php, flow/query.php
```

### 2. 功能增强建议

#### 搜索功能增强
- 添加日期范围选择器（当前为隐藏字段）
- 支持多选条件（多个跟进人员）
- 添加高级搜索功能
- 保存常用搜索条件

#### 导出功能
- 支持Excel导出查询结果
- 支持PDF导出
- 支持打印功能

#### 批量操作
- 批量删除跟进记录
- 批量导出
- 批量修改状态

### 3. 用户体验优化

#### 界面优化
- 使用现代化UI框架（如LayUI已使用）
- 优化移动端显示
- 添加加载动画
- 优化表格显示（固定表头、列宽调整）

#### 交互优化
- 添加确认对话框（删除操作）
- 添加操作成功/失败提示
- 优化分页组件显示
- 添加快速筛选功能

### 4. 技术升级建议

#### 数据库升级
- 迁移到MySQLi或PDO
- 使用预处理语句
- 添加事务支持

#### 前端升级
- 使用Vue.js或React重构前端
- 使用RESTful API
- 实现前后端分离

#### 安全加固
- 实现CSRF防护
- 添加请求频率限制
- 实现操作日志记录
- 添加数据备份机制

---

## 总结

跟进记录查询页面是CRM系统的核心功能之一，实现了基本的查询、分页、排序等功能。但在安全性、性能和代码质量方面还有较大的改进空间。

**主要优点：**
- 功能完整，满足基本业务需求
- 实现了权限控制
- 支持多条件查询
- 界面简洁易用

**主要问题：**
- 存在SQL注入风险
- 存在XSS风险
- 性能优化不足
- 代码结构需要重构

**优先级改进建议：**
1. **高优先级**：修复SQL注入和XSS安全漏洞
2. **中优先级**：优化数据库查询性能，添加索引
3. **低优先级**：代码重构，功能增强

---

## 附录

### A. 相关文件清单

- `list_flow.php` - 列表展示页面
- `query_flow.php` - 查询处理页面
- `flow.php` - 详情页面
- `flow_detail.php` - 跟进记录详情页
- `flow_modify.php` - 跟进记录修改页
- `flow_add.php` - 跟进记录添加页
- `flow_delete.php` - 跟进记录删除页
- `ActionModel.php` - 数据模型类
- `lang.php` - 多语言配置文件

### B. 数据库表关系图

```
inhe_customer_data (客户表)
    │
    │ form_id (流程单号)
    │
    ├──> inhe_customer_data_flow (跟进记录表)
    │       │
    │       │ user_id
    │       │
    └───────┴──> user (用户表)
```

### C. 关键函数说明

- `loadList(page)` - 加载列表数据
- `sortList(obj)` - 列排序
- `pageOperate(url, type)` - 页面操作（详情/修改/删除）
- `getAdminDataQuery(userId, deptId, systemId)` - 获取管理员权限数据
- `queryFlow($param)` - 查询跟进记录

---

**文档版本：** v1.0  
**创建日期：** 2024  
**最后更新：** 2024





# 319
OOP Laravel 
Linux Mysql Redis/MongoDB 数据库优化
小程序制作 Vue/React
* 技术栈：laravel + vue

基础：
单引号和双引号区别？ 
* 双引号串中的内容可以被解释而且替换，而单引号串中的内容只是普通字符。
GET和POST的FORM表单的Action有什么区别？
* get url参数，ASCII编码，中文空格转编码，主动缓存，长度限制，不安全，一个tcp数据包
* post body中，多种编码，2个TCP数据包
Session工作原理
* 服务器产生session对象，并创建一个临时cookie缓存到客户端sessionid，浏览器关闭时临时缓存会被清除，重新生成。
如何取出数组最后一个值？
* end()，array_pop(), array_slice($array,-1,1);
Git分支合并
* git clone, init, add, commit, status, branch & checkout, merge branch,branch -d
翻转字符串"example"
* mb_strlen, mb_substr, krsort, implode
打印出2014-09-23 23:08:01的时间戳
* echo strtotime('2014-09-23 23:08:01');
Debug方法
*  ini_set('display_errors',1); //错误信息
*  ini_set('display_startup_errors',1);    //php启动错误信息
*  error_reporting(-1);                    //打印出所有的 错误信息
Composer用法
* PHP依赖管理工具，使用命令将其他代码引入
* composer.json配置，项目依赖关系和元数据，require确定依赖哪些包
* 包名=供应商名称/项目名称，版本约束
* 安装php composer.phar install，php composer.phar update
* Packagist 资源库
* 自动加载vendor/autoload.php，

进阶：
设计模式：
1. 工厂模式：定义一个创建对象的接口，让其子类自己决定实例化哪一个工厂类，工厂模式使其创建过程延迟到子类进行，主要解决接口选择的问题。让其子类实现工厂接口，返回的也是一个抽象的产品。创建过程在其子类执行。工厂类接口获取对象。
2. 单例模式 - 单例类只能有一个实例。
	单例类必须自己创建自己的唯一实例
	单例类必须给所有其他对象提供这一实例。
3. MVC 存储数据的对象，可视化页面层，控制层控制数据流向模型对象，数据更新时更新页面
框架各部分了解程度（路由、设计规则、数据层包装、视图层、可扩展等）

面向对象的了解程度（静态方法、变量可用范围、抽象类、接口、继承、克隆、魔术方法等）
魔术方法：
1. constuct()构造函数
2. destruct()析构方法
3. clone()克隆：是在克隆一个对象的时候，对新对象进行初始化的工作;instanceof就是用来判断某个对象是否是某个类的一个实例！
4. autoload()自动加载类-当调用不存在的类的时候自动调用！
5. tostring()
6. invoke() - 当将对象当成函数使用的时候自动调用
7. set() - 当对无法访问的属性赋值时自动调用
8. get() - 当对获取无法访问的属性值的时候自动调用
9. unset() - 当销毁无法访问的属性的时候自动调用
10. isset() - 当判断无法访问的属性是否存在的时候自动调用
11. call() - 当调用无法访问的普通方法时自动触发
12. callststic() - 当无法通过静态方法访问时自动触发
命名空间 - namespace
测试用例
异常处理
缓存数据（memcache、redis、模板缓存、apc等各类缓存）
正则测试

高级：
PHP SPL
设计模式（挑选两个，比如工厂、单例讲讲）
排序算法（挑选两个，比如冒泡排序、插入排序讲讲应用点）
1. 选择排序：从数组中选择最小元素，将它与数组的第一个元素交换位置。再从数组剩下的元素中选择出最小的元素，将它与数组的第二个元素交换位置。不断进行这样的操作，直到将整个数组排序。
* 适用于大多数排序场景，虽然他的对比次数较多，但是数据量大的时候，他的效率明显优于冒泡，而且数据移动是非常耗时的，选择排序移动次数少。
2. 冒泡排序： 从左到右不断交换相邻逆序的元素，在一轮的循环之后，可以让未排序的最大元素上浮到右侧。  稳定
在一轮循环中，如果没有发生交换，那么说明数组已经是有序的，此时可以直接退出。
* 使用最简单，适用于数据量很小的排序场景。
3. 插入排序： 每次都将当前元素插入到左侧已经排序的数组中，使得插入之后左侧数组依然有序。 稳定
* 插入排序适用于已有部分数据有序的情况，有序部分越大越好。
4. 快速排序：nlogn 不稳定
加密算法（挑选两个，比如sha、crc32讲讲应用点以及区别）
测试驱动开发
敏捷开发思考
最优开发模式
疑难问题解决方法

安全
分别讲讲XSS、CSRF原理和防范
SQL注入防范
上传应该注意哪些问题
变量覆盖问题
危险函数该如何处理
服务基线
安全域

数据库
事务处理
各类锁处理
分库、主从与离线
左连接、右连接、交叉连接
SQL优化
不常用SQL函数应用
曾经遇到的坑

Linux
挑选两个命令，比如find、awk用法
文件排序方法（比如取某文件夹最后修改的文件并将大小显示为以M为单位的）
python、shell等脚本程度
